package gui;

import java.util.List;

public class PrerpareGameModel {

	boolean getBoard(prepareGameView view) {
		List<String> selectedTopics = view.getUserSelectedTopics();
		if (selectedTopics.size() < 1) {
			// Dialog Box
			System.out.println("Error veze");
		}
		else { 
			//GuiAlgorithmConnector.createPuzzle(); // send request for board
			// show waiting question panel
			//have board, can continue
			
		}
		return true;
	}
}
